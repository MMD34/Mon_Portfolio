# NPM Configuration for Portfolio
# Ensures optional dependencies are installed in local development
# but can be skipped in CI/CD environments

# Install optional dependencies by default (local development)
optional=true

# Ignore scripts that fail (useful for native modules)
ignore-scripts=false

# Engine strict mode (ensure Node version compatibility)
engine-strict=false

# Save exact versions
save-exact=false
